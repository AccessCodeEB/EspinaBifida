# Coverage 100% Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Llevar el coverage de Jest de 94.67% statements / 85.11% branches a 100% en todas las métricas.

**Architecture:** Solo se agregan tests — no se modifica código de producción. Cada tarea ataca un archivo fuente específico con las ramas/líneas sin cubrir. La mayor brecha está en `articulos.model.js` (64%), `migrations/007` (0%) y `membresias.controller.js` (65% branches).

**Tech Stack:** Jest (ESM), Supertest, mockDb helper en `src/tests/helpers/mockDb.js`

---

## Estado actual de coverage

| Archivo | Stmts | Branches | Nota |
|---------|-------|----------|------|
| `articulos.model.js` | 64.1% | 31.81% | CRÍTICO |
| `migrations/007_articulos_activo.js` | 0% | 0% | CRÍTICO |
| `articulos.service.js` | 79.03% | — | ALTO |
| `inventario.model.js` | 81.81% | 55% | ALTO |
| `administradores.controller.js` | 85.36% | — | MEDIO |
| `auth.js` | 87.5% | — | MEDIO |
| `inventario.service.js` | 88% | — | MEDIO |
| `profilePhotosRemoteFallback.js` | 90.9% | — | MEDIO |
| `membresias.controller.js` | 97.67% stmts | 65.38% branches | MEDIO |
| `app.js` | 97.43% stmts | 50% branches | MEDIO |
| varios (services, models, utils) | 93–97% | — | BAJO |

---

## Task 1: articulos.model.js — ORA-00904 fallback paths

**Files:**
- Create: `src/tests/articulos.model.test.js`

Las ramas sin cubrir son las rutas de fallback cuando la columna `ACTIVO` no existe en la tabla (ORA-00904). También faltan: `update` con `setClause` vacío, campo desconocido y filtro null/undefined.

- [ ] **Step 1: Crear el archivo de test con todos los casos**

```js
// src/tests/articulos.model.test.js
import { jest } from "@jest/globals";
import {
  mockExecute, mockClose, dbModuleMock, resetMocks,
} from "./helpers/mockDb.js";

jest.unstable_mockModule("../config/db.js", () => dbModuleMock);

const { findAll, findById, deleteById, update } =
  await import("../models/articulos.model.js");

beforeEach(() => { resetMocks(); });

// ─── findAll ──────────────────────────────────────────────────────────────────

describe("findAll — fallback ORA-00904 (columna ACTIVO no existe)", () => {
  test("usa query sin ACTIVO cuando falla con errorNum 904", async () => {
    const ora904 = Object.assign(
      new Error("ORA-00904: ACTIVO: invalid identifier"),
      { errorNum: 904 }
    );
    mockExecute.mockRejectedValueOnce(ora904);
    mockExecute.mockResolvedValueOnce({ rows: [{ ID_ARTICULO: 1, DESCRIPCION: "X" }] });

    const result = await findAll();

    expect(mockExecute).toHaveBeenCalledTimes(2);
    expect(result).toHaveLength(1);
    // Segunda llamada no debe incluir NVL(ACTIVO
    expect(mockExecute.mock.calls[1][0]).not.toMatch(/ACTIVO/i);
  });

  test("usa fallback cuando el mensaje contiene ORA-00904 (sin errorNum)", async () => {
    const errMsg = new Error("ORA-00904: invalid identifier");
    mockExecute.mockRejectedValueOnce(errMsg);
    mockExecute.mockResolvedValueOnce({ rows: [] });

    const result = await findAll();

    expect(result).toEqual([]);
  });

  test("re-lanza error que NO es ORA-00904", async () => {
    const otroError = Object.assign(
      new Error("ORA-00942: table not found"),
      { errorNum: 942 }
    );
    mockExecute.mockRejectedValueOnce(otroError);

    await expect(findAll()).rejects.toThrow("ORA-00942");
  });
});

// ─── findById ─────────────────────────────────────────────────────────────────

describe("findById — fallback ORA-00904", () => {
  test("usa query sin ACTIVO cuando falla con errorNum 904", async () => {
    const ora904 = Object.assign(new Error("ORA-00904"), { errorNum: 904 });
    mockExecute.mockRejectedValueOnce(ora904);
    mockExecute.mockResolvedValueOnce({ rows: [{ ID_ARTICULO: 1 }] });

    const result = await findById(1);

    expect(result).toHaveProperty("ID_ARTICULO");
    expect(mockExecute.mock.calls[1][0]).not.toMatch(/ACTIVO/i);
  });

  test("fallback retorna null cuando rows está vacío (línea 43: rows[0] ?? null)", async () => {
    const ora904 = Object.assign(new Error("ORA-00904"), { errorNum: 904 });
    mockExecute.mockRejectedValueOnce(ora904);
    mockExecute.mockResolvedValueOnce({ rows: [] });

    const result = await findById(999);

    expect(result).toBeNull();
  });

  test("re-lanza error que NO es ORA-00904 en findById", async () => {
    const otroError = Object.assign(
      new Error("ORA-01031: insufficient privileges"),
      { errorNum: 1031 }
    );
    mockExecute.mockRejectedValueOnce(otroError);

    await expect(findById(1)).rejects.toThrow("ORA-01031");
  });
});

// ─── deleteById ───────────────────────────────────────────────────────────────

describe("deleteById — fallback DELETE cuando ACTIVO no existe", () => {
  test("ejecuta DELETE cuando UPDATE ACTIVO falla con ORA-00904", async () => {
    const ora904 = Object.assign(new Error("ORA-00904"), { errorNum: 904 });
    mockExecute.mockRejectedValueOnce(ora904);
    mockExecute.mockResolvedValueOnce({ rowsAffected: 1 });

    await deleteById(1);

    expect(mockExecute).toHaveBeenCalledTimes(2);
    expect(mockExecute.mock.calls[1][0]).toMatch(/DELETE FROM ARTICULOS/i);
  });

  test("re-lanza error que NO es ORA-00904 en deleteById", async () => {
    const otroError = Object.assign(
      new Error("ORA-01031: insufficient privileges"),
      { errorNum: 1031 }
    );
    mockExecute.mockRejectedValueOnce(otroError);

    await expect(deleteById(1)).rejects.toThrow("ORA-01031");
  });
});

// ─── update ───────────────────────────────────────────────────────────────────

describe("update — setClause vacío (línea 77: if (setClause.length === 0) return)", () => {
  test("retorna sin ejecutar UPDATE cuando el objeto de datos está vacío", async () => {
    const result = await update(1, {});

    // withConnection se llama pero conn.execute NO se llama
    expect(mockExecute).not.toHaveBeenCalled();
    expect(result).toBeUndefined();
  });

  test("registra warning y retorna cuando el campo no existe en dbColumnMap", async () => {
    const warnSpy = jest.spyOn(console, "warn").mockImplementation(() => {});

    const result = await update(1, { campoDesconocido: "valor" });

    expect(warnSpy).toHaveBeenCalledWith(
      expect.stringContaining("Campo desconocido")
    );
    expect(mockExecute).not.toHaveBeenCalled();
    expect(result).toBeUndefined();
    warnSpy.mockRestore();
  });

  test("filtra valores null/undefined y solo actualiza campos con valor", async () => {
    mockExecute.mockResolvedValueOnce({ rowsAffected: 1 });

    await update(1, { descripcion: "Nuevo nombre", unidad: null, cuotaRecuperacion: undefined });

    // Solo DESCRIPCION debe aparecer en el UPDATE, no UNIDAD ni CUOTA_RECUPERACION
    expect(mockExecute).toHaveBeenCalledTimes(1);
    const sql = mockExecute.mock.calls[0][0];
    expect(sql).toMatch(/DESCRIPCION/i);
    expect(sql).not.toMatch(/UNIDAD/i);
  });
});
```

- [ ] **Step 2: Ejecutar el test para verificar que pasa**

```bash
cd /Users/leobardo/Desktop/EspinaBifida
node --experimental-vm-modules node_modules/jest/bin/jest.js src/tests/articulos.model.test.js --no-coverage
```
Expected: All tests PASS

- [ ] **Step 3: Verificar coverage mejorado en articulos.model.js**

```bash
node --experimental-vm-modules node_modules/jest/bin/jest.js --coverage --coverageReporters=text 2>&1 | grep "articulos.model"
```
Expected: cercano a 100%

- [ ] **Step 4: Commit**

```bash
git add src/tests/articulos.model.test.js
git commit -m "test: coverage 100% para articulos.model.js — rutas fallback ORA-00904 y update edge cases"
```

---

## Task 2: inventario.model.js — SP error codes 20001, 20005, generic re-throw

**Files:**
- Modify: `src/tests/inventario.test.js`

Las líneas 25-31 en `applyMovimientoConConexion` manejan `errorNum === 20002 || 20001` (HttpError 422), `errorNum === 20006` (404), `errorNum === 20005` (400), y el catch genérico (re-throw). ORA-20002 y ORA-20006 ya están cubiertos; faltan 20001, 20005 y el re-throw genérico.

- [ ] **Step 1: Agregar tests al describe "POST /api/v1/movimientos" existente**

Leer primero `src/tests/inventario.test.js` para ubicar el describe correcto, luego agregar al final del archivo (antes del `});` de cierre del módulo):

```js
// ─── SP error codes adicionales (inventario.model.js líneas 25-31) ────────────

describe("POST /api/v1/movimientos — códigos de error SP adicionales", () => {
  test("devuelve 422 si SP lanza ORA-20001 (stock insuficiente variante)", async () => {
    const oraErr = Object.assign(
      new Error("ORA-20001: Stock insuficiente para la operacion"),
      { errorNum: 20001 }
    );
    mockExecute.mockRejectedValueOnce(oraErr);

    const res = await request(app)
      .post("/api/v1/movimientos")
      .set("Authorization", `Bearer ${tokenAdmin}`)
      .send({ ...movimientoBase, tipo: "SALIDA", cantidad: 100 });

    expect(res.status).toBe(422);
    expect(res.body.code).toBe("INSUFFICIENT_STOCK");
    expect(mockRollback).toHaveBeenCalled();
  });

  test("devuelve 400 si SP lanza ORA-20005 (tipo de movimiento inválido a nivel DB)", async () => {
    const oraErr = Object.assign(
      new Error("ORA-20005: Tipo de movimiento invalido"),
      { errorNum: 20005 }
    );
    mockExecute.mockRejectedValueOnce(oraErr);

    const res = await request(app)
      .post("/api/v1/movimientos")
      .set("Authorization", `Bearer ${tokenAdmin}`)
      .send(movimientoBase);

    expect(res.status).toBe(400);
    expect(res.body.code).toBe("INVALID_MOVIMIENTO_TIPO");
    expect(mockRollback).toHaveBeenCalled();
  });

  test("propaga error genérico del SP como 500", async () => {
    const genericErr = new Error("ORA-01031: insufficient privileges");
    mockExecute.mockRejectedValueOnce(genericErr);

    const res = await request(app)
      .post("/api/v1/movimientos")
      .set("Authorization", `Bearer ${tokenAdmin}`)
      .send(movimientoBase);

    expect(res.status).toBe(500);
    expect(mockRollback).toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: Ejecutar**

```bash
node --experimental-vm-modules node_modules/jest/bin/jest.js src/tests/inventario.test.js --no-coverage
```
Expected: todos los tests PASS

- [ ] **Step 3: Commit**

```bash
git add src/tests/inventario.test.js
git commit -m "test: coverage para inventario.model.js — SP error codes 20001, 20005 y re-throw genérico"
```

---

## Task 3: articulos.service.js — validaciones de stockMinimo e idArticulo NaN

**Files:**
- Modify: `src/tests/articulos.test.js`

`normalizeData` en `articulos.service.js` tiene validadores para `stockMinimo` (líneas 42-46) e `idArticulo` NaN (línea 13) que no son ejercidos por los tests actuales.

- [ ] **Step 1: Agregar tests al describe "POST /api/v1/articulos" existente**

Leer `src/tests/articulos.test.js` primero para ubicar el describe `"POST /api/v1/articulos — crear artículo"`, luego agregar antes del `});` de cierre:

```js
  test("falla si stockMinimo es texto (NaN) → 400", async () => {
    const res = await request(app)
      .post("/api/v1/articulos")
      .set("Authorization", `Bearer ${tokenAdmin}`)
      .send({ ...articuloBase, stockMinimo: "abc" });

    expect(res.status).toBe(400);
    expect(res.body.error).toMatch(/stockMinimo/i);
  });

  test("falla si stockMinimo es negativo → 400", async () => {
    const res = await request(app)
      .post("/api/v1/articulos")
      .set("Authorization", `Bearer ${tokenAdmin}`)
      .send({ ...articuloBase, stockMinimo: -1 });

    expect(res.status).toBe(400);
    expect(res.body.error).toMatch(/stockMinimo/i);
  });

  test("falla si idArticulo no es numérico (NaN) → 400", async () => {
    const res = await request(app)
      .post("/api/v1/articulos")
      .set("Authorization", `Bearer ${tokenAdmin}`)
      .send({ ...articuloBase, idArticulo: "not-a-number" });

    expect(res.status).toBe(400);
    expect(res.body.error).toMatch(/idArticulo/i);
  });

  test("acepta stockMinimo = 0 (borde válido)", async () => {
    mockExecute.mockResolvedValueOnce({ rowsAffected: 1 });

    const res = await request(app)
      .post("/api/v1/articulos")
      .set("Authorization", `Bearer ${tokenAdmin}`)
      .send({ ...articuloBase, stockMinimo: 0 });

    expect(res.status).toBe(201);
  });
```

- [ ] **Step 2: Ejecutar**

```bash
node --experimental-vm-modules node_modules/jest/bin/jest.js src/tests/articulos.test.js --no-coverage
```
Expected: todos los tests PASS

- [ ] **Step 3: Commit**

```bash
git add src/tests/articulos.test.js
git commit -m "test: coverage para articulos.service.js — validaciones stockMinimo e idArticulo NaN"
```

---

## Task 4: migrations/007_articulos_activo.js — cobertura completa (0% → 100%)

**Files:**
- Modify: `src/tests/migrations.test.js`

Toda la migración 007 está sin cubrir. Patrón igual al resto de migraciones en el mismo archivo.

- [ ] **Step 1: Agregar import y tests al final de migrations.test.js**

Leer `src/tests/migrations.test.js` primero para confirmar el final del archivo, luego agregar:

```js
// ═══════════════════════════════════════════════════════════════════════════════
// runMigration007 — Agrega columna ACTIVO a ARTICULOS
// ═══════════════════════════════════════════════════════════════════════════════

const { runMigration007 } = await import("../migrations/007_articulos_activo.js");

describe("runMigration007 — columna ACTIVO ya existe", () => {
  test("retorna sin ejecutar ALTER si la columna ya existe", async () => {
    // columnExists → SELECT USER_TAB_COLUMNS → TOTAL = 1
    mockExecute.mockResolvedValueOnce({ rows: [{ TOTAL: 1 }] });

    const logSpy = jest.spyOn(console, "log").mockImplementation(() => {});

    await runMigration007();

    expect(mockExecute).toHaveBeenCalledTimes(1);
    expect(mockClose).toHaveBeenCalledTimes(1);
    // No ALTER ejecutado
    expect(mockExecute.mock.calls[0][0]).toMatch(/USER_TAB_COLUMNS/i);
    logSpy.mockRestore();
  });
});

describe("runMigration007 — columna ACTIVO no existe (estado inicial)", () => {
  test("ejecuta ALTER TABLE ADD ACTIVO cuando la columna no existe", async () => {
    // columnExists → TOTAL = 0 → must run ALTER
    mockExecute.mockResolvedValueOnce({ rows: [{ TOTAL: 0 }] });
    mockExecute.mockResolvedValueOnce({}); // ALTER TABLE

    const logSpy = jest.spyOn(console, "log").mockImplementation(() => {});

    await runMigration007();

    expect(mockExecute).toHaveBeenCalledTimes(2);
    const alter = mockExecute.mock.calls[1][0].trim();
    expect(alter).toMatch(/ALTER TABLE ARTICULOS/i);
    expect(alter).toMatch(/ACTIVO CHAR\(1\)/i);
    expect(mockClose).toHaveBeenCalledTimes(1);
    logSpy.mockRestore();
  });
});

describe("runMigration007 — error durante ALTER TABLE", () => {
  test("cierra conexión y relanza el error", async () => {
    mockExecute.mockResolvedValueOnce({ rows: [{ TOTAL: 0 }] }); // columna no existe
    mockExecute.mockRejectedValueOnce(new Error("ORA-01031: insufficient privileges"));

    await expect(runMigration007()).rejects.toThrow("ORA-01031");

    expect(mockClose).toHaveBeenCalledTimes(1);
  });
});

describe("runMigration007 — falla al obtener conexión", () => {
  test("lanza el error sin intentar cerrar conexión nula", async () => {
    dbModuleMock.getConnection.mockRejectedValueOnce(
      new Error("ORA-12541: TNS:no listener")
    );

    await expect(runMigration007()).rejects.toThrow("ORA-12541");

    expect(mockClose).not.toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: Ejecutar**

```bash
node --experimental-vm-modules node_modules/jest/bin/jest.js src/tests/migrations.test.js --no-coverage
```
Expected: todos los tests PASS

- [ ] **Step 3: Commit**

```bash
git add src/tests/migrations.test.js
git commit -m "test: coverage 100% para migrations/007_articulos_activo.js"
```

---

## Task 5: auth.js — ramas sin cubrir

**Files:**
- Modify: `src/tests/core-coverage.test.js`

Ramas que faltan:
- `checkRole` cuando `!req.user` (línea 29) — posible solo en unit test porque en HTTP siempre viene de `verifyToken`
- `verifyToken`: authHeader no empieza con "Bearer " → token = null
- `verifyToken`: `TokenExpiredError` (línea 15)

- [ ] **Step 1: Agregar tests al final de core-coverage.test.js**

Leer `src/tests/core-coverage.test.js` primero para confirmar la ubicación, luego agregar (los imports ya están al inicio del archivo):

```js
// ─── auth middleware (directo) ────────────────────────────────────────────────
import { verifyToken, checkRole } from "../middleware/auth.js";
import jwt from "jsonwebtoken";

const TEST_JWT_SECRET = process.env.JWT_SECRET ?? "test-secret-espina-bifida";

describe("auth middleware — ramas adicionales", () => {
  function makeReqRes() {
    const req = { headers: {} };
    const res = {
      statusCode: 200,
      body: undefined,
      status(code) { this.statusCode = code; return this; },
      json(payload) { this.body = payload; return this; },
    };
    const next = jest.fn();
    return { req, res, next };
  }

  test("verifyToken: authHeader existe pero no empieza con 'Bearer ' → 401", () => {
    const { req, res, next } = makeReqRes();
    req.headers["authorization"] = "Basic abc123";

    verifyToken(req, res, next);

    expect(res.statusCode).toBe(401);
    expect(res.body.error).toBe("Token no proporcionado");
    expect(next).not.toHaveBeenCalled();
  });

  test("verifyToken: token expirado → 401 'Token expirado'", () => {
    const expiredToken = jwt.sign(
      { idAdmin: 1, idRol: 1 },
      TEST_JWT_SECRET,
      { expiresIn: -1 }
    );
    const { req, res, next } = makeReqRes();
    req.headers["authorization"] = `Bearer ${expiredToken}`;

    verifyToken(req, res, next);

    expect(res.statusCode).toBe(401);
    expect(res.body.error).toBe("Token expirado");
    expect(next).not.toHaveBeenCalled();
  });

  test("checkRole: req.user no definido → 401 'No autenticado'", () => {
    const middleware = checkRole(1, 2);
    const { req, res, next } = makeReqRes();
    req.user = undefined;

    middleware(req, res, next);

    expect(res.statusCode).toBe(401);
    expect(res.body.error).toBe("No autenticado");
    expect(next).not.toHaveBeenCalled();
  });
});
```

**Nota:** Si `verifyToken` y `checkRole` ya están importados en el archivo (no lo están por defecto), agregar los imports al inicio del archivo junto a los demás.

- [ ] **Step 2: Ejecutar**

```bash
node --experimental-vm-modules node_modules/jest/bin/jest.js src/tests/core-coverage.test.js --no-coverage
```
Expected: todos los tests PASS

- [ ] **Step 3: Commit**

```bash
git add src/tests/core-coverage.test.js
git commit -m "test: coverage para auth.js — checkRole sin req.user, header non-Bearer, TokenExpiredError"
```

---

## Task 6: profilePhotosRemoteFallback.js — ramas de error sin cubrir

**Files:**
- Modify: `src/tests/profile-photos-fallback.test.js`

Ramas que faltan:
- Línea 34–35: callback de `res.sendFile` con error → `next(err)`. Para testear: espiar `fs.promises.access` para que resuelva (fingir que el archivo existe) pero el archivo real no existe → Express llama el callback con ENOENT.
- Línea 56: buffer vacío retornado por remote → `res.status(404).end()`

- [ ] **Step 1: Agregar dos tests al final del describe en profile-photos-fallback.test.js**

Leer `src/tests/profile-photos-fallback.test.js` primero para confirmar el patrón y el cierre del describe, luego agregar:

```js
  test("sendFile callback con error delega a errorHandler (siguiente middleware)", async () => {
    // Fingir que el archivo existe (fs.access resuelve)
    // pero el archivo NO existe en disco → sendFile llama callback con ENOENT
    // Resultado: el error handler devuelve 404 o 500
    const { promises: fsPromises } = await import("node:fs");
    const accessSpy = jest.spyOn(fsPromises, "access").mockResolvedValueOnce(undefined);

    const name = `zz-sendfile-err-${Date.now()}.jpg`;
    // El archivo no existe en disco

    const res = await request(app).get(`/uploads/profiles/${name}`);

    // El error handler responde con un status de error (no 200)
    expect(res.status).not.toBe(200);
    accessSpy.mockRestore();
  });

  test("buffer vacío desde remoto retorna 404 (línea 56: if (!buf.length))", async () => {
    process.env.PROFILE_PHOTOS_REMOTE_BASE = "http://example.invalid";
    const fetchSpy = jest.spyOn(global, "fetch").mockResolvedValue({
      ok: true,
      status: 200,
      arrayBuffer: async () => new ArrayBuffer(0), // buffer vacío
      headers: { get: () => null },
    });

    const res = await request(app).get(
      `/uploads/profiles/zz-empty-buf-${Date.now()}.jpg`
    );

    expect(res.status).toBe(404);
    fetchSpy.mockRestore();
  });
```

- [ ] **Step 2: Ejecutar**

```bash
node --experimental-vm-modules node_modules/jest/bin/jest.js src/tests/profile-photos-fallback.test.js --no-coverage
```
Expected: todos los tests PASS

**Nota:** Si el test de `sendFile` es inestable (timing issues), reemplazar con un unit test directo del middleware que pase un mock `res.sendFile` que llame el callback con error.

- [ ] **Step 3: Commit**

```bash
git add src/tests/profile-photos-fallback.test.js
git commit -m "test: coverage para profilePhotosRemoteFallback.js — sendFile error callback y empty buffer"
```

---

## Task 7: app.js CORS branch + administradores.controller.js uploadFotoPerfil

**Files:**
- Modify: `src/tests/health.test.js`
- Modify: `src/tests/controllers-misc.test.js`

`app.js` tiene 50% branch coverage en la función CORS origin (líneas 33-36). Los tests actuales no envían header `Origin`, por lo que solo la rama `!origin === true` se ejecuta.

`administradores.controller.js` tiene `uploadFotoPerfil` (líneas 88-101) que requiere testear el path cuando `!req.file` (línea 90).

- [ ] **Step 1: Agregar test de CORS al health test**

Leer `src/tests/health.test.js` primero, luego agregar:

```js
describe("CORS origin callback (app.js línea 33-36)", () => {
  test("responde 200 cuando la request incluye header Origin (rama non-null origin)", async () => {
    const { default: request } = await import("supertest");
    const { default: app } = await import("../app.js");

    const res = await request(app)
      .get("/health")
      .set("Origin", "http://localhost:3000");

    expect(res.status).toBe(200);
    expect(res.headers["access-control-allow-origin"]).toBeDefined();
  });
});
```

**Nota:** Si `health.test.js` ya importa `app` y `request` en el scope, usar esas referencias directamente sin redeclararlas.

- [ ] **Step 2: Agregar test de uploadFotoPerfil sin archivo a controllers-misc.test.js**

Leer `src/tests/controllers-misc.test.js` para confirmar el tokenAdmin y la estructura, luego agregar un nuevo describe al final:

```js
// ─── administradores.controller.js — uploadFotoPerfil sin archivo ─────────────

describe("POST /api/v1/administradores/:idAdmin/foto — sin archivo adjunto", () => {
  test("devuelve 400 si no se envía imagen (req.file ausente)", async () => {
    const res = await request(app)
      .post("/api/v1/administradores/1/foto")
      .set("Authorization", `Bearer ${tokenAdmin}`);
      // sin .attach() → req.file será undefined

    expect(res.status).toBe(400);
    expect(res.body.code).toBe("MISSING_FILE");
  });
});
```

- [ ] **Step 3: Ejecutar ambos tests**

```bash
node --experimental-vm-modules node_modules/jest/bin/jest.js src/tests/health.test.js src/tests/controllers-misc.test.js --no-coverage
```
Expected: todos PASS

- [ ] **Step 4: Commit**

```bash
git add src/tests/health.test.js src/tests/controllers-misc.test.js
git commit -m "test: coverage para app.js CORS origin branch y administradores.controller uploadFotoPerfil"
```

---

## Task 8: membresias.controller.js — branch coverage (65.38% → 100%)

**Files:**
- Modify: `src/tests/membresias.controller.test.js`

`membresias.controller.js` tiene muchas expresiones `??` y `!= null` con ramas no cubiertas:
- `formatMonto(null)` → returns null (línea 5)
- `formatMonto(NaN)` → returns null (línea 7)
- `mapMembresia` con `estatusMembresia` nulo → "Vencida" (línea 12)
- `mapMembresia` con `diasRestantes` nulo → null (línea 13)
- `mapMembresia` con `fechaUltimoPago` nulo → usa `fechaVigenciaInicio` (línea 21)
- `mapPago` con campos nulos

- [ ] **Step 1: Leer membresias.controller.test.js para entender la estructura actual**

Leer `src/tests/membresias.controller.test.js` completamente antes de modificar.

- [ ] **Step 2: Agregar un describe con rows que ejerciten los ?? null branches**

Agregar al final del archivo de test:

```js
// ─── branch coverage para mapMembresia / mapPago / formatMonto ───────────────

describe("GET /api/v1/membresias — ramas de mapeo nulo", () => {
  test("mapMembresia maneja row con estatusMembresia nulo → 'Vencida', diasRestantes nulo → null", async () => {
    mockExecute.mockResolvedValueOnce({
      rows: [{
        CURP:                  "GAJE900101HMNABC01",
        NOMBRE_COMPLETO:       "Ana Test",
        NUMERO_CREDENCIAL:     "C-999",
        FECHA_VIGENCIA_INICIO: "2025-01-01",
        FECHA_VIGENCIA_FIN:    "2025-12-31",
        ESTATUS_MEMBRESIA:     null,   // → "Vencida"
        DIAS_RESTANTES:        null,   // → null
        FECHA_ULTIMO_PAGO:     null,   // → usa fechaVigenciaInicio
        MONTO:                 null,   // → formatMonto(null) → null
        METODO_PAGO:           null,   // → null
        REFERENCIA:            null,   // → null
        OBSERVACIONES:         null,
      }],
    });

    const res = await request(app)
      .get("/api/v1/membresias")
      .set("Authorization", `Bearer ${tokenAdmin}`);

    expect(res.status).toBe(200);
    expect(res.body[0].estatus).toBe("Vencida");
    expect(res.body[0].diasRestantes).toBeNull();
    expect(res.body[0].monto).toBeNull();
    expect(res.body[0].metodoPago).toBeNull();
  });

  test("mapMembresia con diasRestantes no nulo → floor(Number(valor))", async () => {
    mockExecute.mockResolvedValueOnce({
      rows: [{
        CURP:                  "GAJE900101HMNABC02",
        NOMBRE_COMPLETO:       "Juan Test",
        NUMERO_CREDENCIAL:     "C-998",
        FECHA_VIGENCIA_INICIO: "2025-01-01",
        FECHA_VIGENCIA_FIN:    "2026-12-31",
        ESTATUS_MEMBRESIA:     "Activo",
        DIAS_RESTANTES:        "45.7",  // → Math.floor(45.7) = 45
        FECHA_ULTIMO_PAGO:     "2025-01-01",
        MONTO:                 "abc",   // → Number("abc") = NaN → null (formatMonto NaN branch)
        METODO_PAGO:           "Efectivo",
        REFERENCIA:            "REF-001",
        OBSERVACIONES:         null,
      }],
    });

    const res = await request(app)
      .get("/api/v1/membresias")
      .set("Authorization", `Bearer ${tokenAdmin}`);

    expect(res.status).toBe(200);
    expect(res.body[0].diasRestantes).toBe(45);
    expect(res.body[0].monto).toBeNull(); // NaN branch de formatMonto
  });
});

describe("GET /api/v1/membresias/pagos-recientes — ramas de mapPago nulo", () => {
  test("mapPago maneja row con metodoPago, referencia y observaciones nulos", async () => {
    mockExecute.mockResolvedValueOnce({
      rows: [{
        ID_CREDENCIAL:         1,
        CURP:                  "GAJE900101HMNABC01",
        NOMBRE_COMPLETO:       "Ana Test",
        FECHA_EMISION:         "2025-01-01",
        FECHA_VIGENCIA_INICIO: "2025-01-01",
        FECHA_VIGENCIA_FIN:    "2025-12-31",
        FECHA_ULTIMO_PAGO:     "2025-01-01",
        MONTO:                 null,     // → null
        METODO_PAGO:           null,     // → null
        REFERENCIA:            null,     // → null
        OBSERVACIONES:         null,     // → null
      }],
    });

    const res = await request(app)
      .get("/api/v1/membresias/pagos-recientes")
      .set("Authorization", `Bearer ${tokenAdmin}`);

    expect(res.status).toBe(200);
    expect(res.body[0].metodoPago).toBeNull();
    expect(res.body[0].referencia).toBeNull();
    expect(res.body[0].observaciones).toBeNull();
  });
});
```

- [ ] **Step 3: Ejecutar**

```bash
node --experimental-vm-modules node_modules/jest/bin/jest.js src/tests/membresias.controller.test.js --no-coverage
```
Expected: todos PASS

- [ ] **Step 4: Commit**

```bash
git add src/tests/membresias.controller.test.js
git commit -m "test: branch coverage 100% para membresias.controller.js — ramas null coalescing"
```

---

## Task 9: Gaps restantes — run coverage HTML y cubrir líneas específicas

**Files:**
- Varios (ver instrucciones abajo)

Después de las tareas anteriores, aún puede haber pequeños gaps en:
- `inventario.service.js` — `assertArticuloSinMovimientos` total > 0
- `membresias.service.js`, `servicios.service.js`, `citas.service.js` — pequeñas ramas
- `utils/validators.js`, `utils/reporteTemplate.js` — pequeñas ramas
- `middleware/errorHandler.js` — ramas adicionales

- [ ] **Step 1: Ejecutar coverage completo y generar reporte HTML**

```bash
cd /Users/leobardo/Desktop/EspinaBifida
node --experimental-vm-modules node_modules/jest/bin/jest.js --coverage --coverageReporters=text 2>&1 | tee /tmp/coverage-report.txt
```

- [ ] **Step 2: Identificar archivos con coverage < 100%**

```bash
grep -E "^\s*[^|].*\s+[0-9]+\.[0-9]+\s+\|" /tmp/coverage-report.txt | grep -v "100 |" | head -40
```

- [ ] **Step 3: Para cada archivo con coverage < 100%, leer el archivo fuente**

Para cada archivo listado, ejecutar:
```bash
node --experimental-vm-modules node_modules/jest/bin/jest.js --coverage --coverageReporters=json 2>/dev/null
# Luego leer coverage/coverage-summary.json para ver exactamente qué líneas faltan
```

O abrir `coverage/lcov-report/index.html` para ver las líneas sin cubrir marcadas en rojo.

- [ ] **Step 4: Cubrir assertArticuloSinMovimientos (inventario.service.js)**

Grep para encontrar dónde se llama:
```bash
grep -r "assertArticuloSinMovimientos" src/ --include="*.js" -n
```

Si se llama desde un endpoint (ej. DELETE /articulos), agregar un test que haga trigger del `total > 0` path. Si no está conectado a ningún endpoint, agregar un test de servicio directo:

```js
// En un nuevo describe en inventario.test.js o en articulos.test.js
// (solo si assertArticuloSinMovimientos es llamada desde la ruta DELETE /articulos):

test("DELETE /api/v1/articulos/:id → 409 si el artículo tiene movimientos registrados", async () => {
  // findById controller → existe, stock=0
  mockExecute.mockResolvedValueOnce({ rows: [{ ...articuloRowSinStock }] });
  // findById service → existe, stock=0
  mockExecute.mockResolvedValueOnce({ rows: [{ ...articuloRowSinStock }] });
  // countMovimientosByArticulo → TOTAL = 5 (tiene movimientos)
  mockExecute.mockResolvedValueOnce({ rows: [{ TOTAL: 5 }] });

  const res = await request(app)
    .delete("/api/v1/articulos/101")
    .set("Authorization", `Bearer ${tokenAdmin}`);

  expect(res.status).toBe(409);
  expect(res.body.code).toBe("ARTICULO_HAS_MOVIMIENTOS");
});
```

- [ ] **Step 5: Para cada brecha restante, escribir el test mínimo que cubre la línea**

Seguir el patrón de los tests existentes. Regla: leer el archivo fuente, identificar la condición no ejercida, escribir el test más simple que la active.

- [ ] **Step 6: Ejecutar todos los tests**

```bash
node --experimental-vm-modules node_modules/jest/bin/jest.js --no-coverage
```
Expected: 0 failures

- [ ] **Step 7: Commit acumulado**

```bash
git add src/tests/
git commit -m "test: cubrir gaps restantes identificados en HTML coverage report"
```

---

## Task 10: Verificación final — 100% coverage

**Files:**
- Ninguno (solo verificación)

- [ ] **Step 1: Ejecutar coverage completo**

```bash
cd /Users/leobardo/Desktop/EspinaBifida
node --experimental-vm-modules node_modules/jest/bin/jest.js --coverage --coverageReporters=text 2>&1 | tail -40
```

Expected:
```
All files | 100 | 100 | 100 | 100
```

- [ ] **Step 2: Si algún archivo < 100%, leer el HTML report e identificar la línea exacta**

```bash
open coverage/lcov-report/index.html
```
Buscar archivos en rojo, leer las líneas no cubiertas, agregar test mínimo.

- [ ] **Step 3: Si import.meta.url guard (línea standalone) no se puede cubrir**

Para archivos como `007_articulos_activo.js` que tienen el bloque:
```js
if (import.meta.url === `file://${process.argv[1]}`) {
  runMigration007().catch(...)
}
```
Esta rama (true) nunca se activa en tests. Si sigue sin cubrirse, agregar comentario de exclusión SOLO en esa línea:
```js
/* c8 ignore next 4 */
if (import.meta.url === `file://${process.argv[1]}`) {
```
O actualizar `jest.config.js` para excluir esa rama específica usando `coveragePathIgnorePatterns` si aplica.

- [ ] **Step 4: Actualizar threshold en jest.config.js (opcional)**

Si se alcanza 100%, actualizar el threshold:
```js
coverageThreshold: {
  global: {
    lines: 100,
    branches: 100,
    statements: 100,
    functions: 100,
  }
},
```

- [ ] **Step 5: Commit final**

```bash
git add jest.config.js  # si se modificó
git commit -m "test: coverage al 100% en todas las métricas"
```

---

## Resumen de archivos modificados/creados

| Acción | Archivo |
|--------|---------|
| CREATE | `src/tests/articulos.model.test.js` |
| MODIFY | `src/tests/articulos.test.js` |
| MODIFY | `src/tests/inventario.test.js` |
| MODIFY | `src/tests/migrations.test.js` |
| MODIFY | `src/tests/core-coverage.test.js` |
| MODIFY | `src/tests/profile-photos-fallback.test.js` |
| MODIFY | `src/tests/health.test.js` |
| MODIFY | `src/tests/controllers-misc.test.js` |
| MODIFY | `src/tests/membresias.controller.test.js` |
| MODIFY | `src/tests/*.test.js` (varios, Task 9) |
